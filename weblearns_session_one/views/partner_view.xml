<odoo>

    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">partner.form.view</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="vendor_priority" />
                <field name="is_filter_po_products_category" />
                <field name="prd_list_ids" invisible="is_filter_po_products_category == False"
                    widget="many2many_tags"/>
            </xpath>
        </field>
    </record>

     <record id="view_res_partner_filter" model="ir.ui.view">
        <field name="name">partner.filter.view</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter" />
        <field name="arch" type="xml">
            <xpath expr="//group" position="inside">
                <filter name="vendor_priority" string="Vendor Priority" domain="[]" context="{'group_by' : 'vendor_priority'}"/>
            </xpath>
        </field>
    </record>

</odoo>
